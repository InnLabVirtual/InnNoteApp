import { StyleSheet } from 'react-native'
import theme from './../../../styles/theme.style'

export default StyleSheet.create({
  emphatize: {
  },
  define: {
    marginTop: theme.MARGIN_PHASEMAP

  },
  ideate: {

  },
  prototyp: {
    marginTop: theme.MARGIN_PHASEMAP
  },
  test: {
    marginTop: theme.MARGIN_PHASEMAP * 2

  }
})
